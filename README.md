# ChatCodeAngular with o4-mini (preview)
In this example we used GitHub Copilot "agent" mode with o4-mini (preview) to: 
- generate an app that searches Wikipedia articles and displays results in a list format using Angular

## Prompts used
The prompts used in this example are available in the [prompts.md](prompts.md) file.

## Results
### App
The app does not work even after few iterations.
### Architecture
The design is not easy for me to understand.

## Run the app
To run the app, launch the following command in the terminal:
```bash
ng serve
```
Then open your browser and navigate to `http://localhost:4200/`.\
As I said it does not work as expected.
